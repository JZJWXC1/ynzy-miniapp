# AGENTS.md

## 语言设置 / Language Setting

**永远使用中文交流**：所有回复、解释和代码注释都必须使用中文，无论代码本身使用什么语言。

- 对话交流使用中文。
- 代码解释使用中文。
- 代码注释使用中文。
- 输出格式永远保持开头带一个 emoji，结尾带上“嘻嘻”。

## 第一版产品规则

1. 第一版只面向中介使用，暂不开放租客端，也不扩展房东或其他角色。
2. 产品目标是简单、高效，让中介快速找到真实可租房源。
3. 底部导航固定为：找房、房源、地图、我的。
4. 房源群、积分、充值、换群、微信支付第一版隐藏，但不要删除后端历史代码。
5. 地图找房必须保留，并作为核心功能。
6. 地图只能展示真实且经过确认的小区坐标。
7. 禁止使用随机坐标、散列坐标、区域估算坐标、默认中心点冒充真实位置。
8. 无可靠坐标的房源可以进入普通列表，但不能进入地图。
9. 地图只展示小区位置，不能展示具体楼栋、单元、房号和房东电话。
10. 房态规则固定为：第 3 天提醒、第 5 天再次提醒、第 7 天未更新自动失效。
11. 前台只展示有效房源，失效房源保留在后台资产池。
12. 第一版报备保持简单：客户称呼可选，客户手机号必填。
13. 签单只能从报备记录发起。
14. 签单只填写：成交月租、房东实际支付佣金、可选备注。
15. 上传房源的中介获得房东实际支付佣金的 20%。
16. 计算公式：上传人分佣 = 房东实际支付佣金 × 20%。
17. 20% 由后端固定计算，前端不能修改。
18. 金额统一按分存储，避免小数误差。
19. 管理员确认签单后才生成正式分佣记录。
20. 不信任客户端提交的 brokerId、uploaderId、commissionRate。
21. 不在日志中输出完整客户手机号、房东电话、微信号或身份证信息。
22. 不把密钥写入仓库。
23. 优先复用现有架构，不做无关重构。
24. 不随意增加生产依赖。

## 并行任务约束

1. 并行任务禁止修改同一文件。
2. 不允许修改任务范围之外的文件。
3. 并行任务禁止修改 `server/scripts/smoke-test.js`。
4. 发现必须跨范围修改时，只记录为集成事项，不擅自修改。
5. 每个任务结束必须输出：
   - 修改文件
   - 接口变化
   - 数据结构变化
   - 测试结果
   - 未解决问题
   - 微信开发者工具验收步骤
6. 每个任务结束必须执行 `git diff --name-only`，并检查是否越界。

## CodeGraph

如果仓库根目录存在 `.codegraph/`，在需要理解或定位代码时，优先使用 CodeGraph，再考虑文本搜索或直接读取文件。
