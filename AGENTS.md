# AGENTS.md

## 语言设置 / Language Setting

**永远使用中文交流**：所有回复、解释和代码注释都必须使用中文，无论代码本身使用什么语言。

- 对话交流使用中文。
- 代码解释使用中文。
- 代码注释使用中文。
- 输出格式永远保持开头带一个 emoji，结尾带上“嘻嘻”。

## 第一版范围

1. 第一版只面向中介，不面向房东、租客或其他角色扩展。
2. 底部导航固定为：找房、房源、地图、我的。
3. 房源群、积分、充值、微信支付入口全部隐藏。
4. 地图只允许使用真实且已确认的小区坐标。
5. 禁止随机坐标、散列坐标、区域估算坐标和默认中心坐标进入地图。
6. 房态规则固定为：第 3 天提醒、第 5 天提醒、第 7 天失效。
7. 报备保持简单，不做复杂审批流或额外角色流程。
8. 上传房源中介分佣固定为房东实际支付佣金的 20%。

## 并行任务约束

1. 并行任务禁止修改相同文件。
2. 禁止修改任务范围之外的文件。
3. 禁止并行任务修改 `server/scripts/smoke-test.js`。
4. 每个任务结束必须执行 `git diff --name-only`，并检查是否出现越界文件。

## CodeGraph

如果仓库根目录存在 `.codegraph/`，在需要理解或定位代码时，优先使用 CodeGraph，再考虑文本搜索或直接读取文件。
