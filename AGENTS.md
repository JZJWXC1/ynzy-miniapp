# AGENTS.md

## 语言与交流

- 永远使用中文交流，代码解释和新增代码注释也使用中文。
- 对外回复开头带一个 emoji，结尾带“嘻嘻”。
- 如果仓库根目录存在 `.codegraph/`，理解或定位代码时优先使用 CodeGraph，再考虑文本搜索或直接读文件。

## 红线

- 不修改 `server/scripts/smoke-test.js`，除非用户明确点名要求。
- `server/data/`、`server/certs/`、任何 `.env`、`lark-*.json`、`project.private.config.json` 不进提交、不进部署包、不进外发产物。
- 任何密钥、Token、AppSecret、上传私钥、证书私钥、真实生产数据都不得写入仓库或聊天输出。
- 不主动 push。完成本地 commit 后等待用户确认；只有用户明确要求 push 时才执行。
- 不信任客户端提交的登录身份、上传人、分佣比例或敏感权限字段，相关规则必须由服务端固定计算。

## 工作流

- 改动前先跑当前基线测试，确认不是在未知失败状态上继续叠改。
- 一项改动一个 commit，commit 信息简短说明业务目的。
- 每轮改完跑全量 V1 测试：执行 `server/scripts/*-test.js` 中除 `smoke-test.js` 以外的测试，再执行 `server/scripts/v1-final-audit.js`。
- PowerShell 参考命令：

```powershell
Push-Location server
Get-ChildItem scripts -Filter "*-test.js" | Where-Object { $_.Name -ne "smoke-test.js" } | ForEach-Object { node $_.FullName }
node scripts/v1-final-audit.js
Pop-Location
```

- 部署可自动执行（需用户提供目标服务器与凭据）。无论人工还是自动，部署前后都要核对生产 `listings` 总数并解释差异。
- 生成部署包后必须执行 `docs/打包与脱敏清单.md` 第四节的自查命令；有任何命中即打包失败。
- 发现本地有用户或其他协作者的未提交改动时，不要回滚；先确认影响范围，必要时在同一文件内谨慎合并。

## 验证要求

- 验收断言必须固化进测试脚本或审计脚本，不靠口头承诺。
- 接口边界、权限边界、敏感字段剥离、分佣结果、筛选组合、地图坐标来源这类规则都要有可重复验证。
- 真机问题需要给出链路证据：前端授权或报错、公网接口或 WebSocket 状态、后端日志或配置点。
- 遇到“后端查无请求、前端只报统一网络错误”这类真机问题时，第一时间打开 vConsole 取证，先记录页面路由、生命周期时序、Network/console 原始 `errMsg` 和请求 URL/超时配置，再判断是请求未发出、未到 Nginx、未到应用还是后端兜底失效；禁止在没有 vConsole 时序证据时反复按假设改。

## 找房助手产品规则

- 用户问“XX 附近有没有”时，先查平台已知词条：若 `XX` 命中已配置板块、区域或小区，直接按该范围筛选，不进入“坐标未确认”追问。
- 若没有精确词条但有相近词条，先追问“是不是指 XX”；若完全无匹配，优先用平台地图/范围能力给出最接近的真实房源兜底，不反问用户补地址。
- 语音输入只负责“语音转文字并填入输入框”；文字找房请求必须独立发起，不得依赖录音器、ASR WebSocket 或语音回调状态。

## 文档同步

- 业务规则、接口参数、部署方式、环境变量、数据结构或安全边界变化时，必须同步更新 `server/README.md`。
- 影响当前交接状态的变化，也要同步更新 `docs/交接报告-20260704.md`。
- 历史预留能力可以保留代码，但文档必须标明第一版是否生效，避免新协作者误开旧入口。

## 有限并行工作树

- 每个子任务开工前先声明将修改的文件清单；两个并行任务文件集有交集时必须串行。
- `server/src/domain.js`、`server/src/index.js`、数据库数据结构视为独占资源，同一时刻只允许一个工作树修改。
- 每个工作树使用独立分支，命名为 `wt/任务名`；合并回 `v1-broker` 前必须 rebase 到最新，并重跑全量测试（排除 `smoke-test.js`）和 `server/scripts/v1-final-audit.js`。
- 多批次任务必须按用户指定批次顺序合并，禁止子任务分支之间互相 merge。
- 部署包只允许从合并后的 `v1-broker` 生成，禁止从子工作树出包。
- 每个并行工作树必须配一个独立只读审计步骤：合并前全文自查 diff，确认密钥、`data/`、`certs/` 未混入，无新增裸 `innerHTML`，无客户端可控的权限与分佣字段。

## AI 协作会话

- Claude Code 作为主开发、Codex 作为第二裁判时，双方优先使用 `docs/AI协作会话.md` 做共享交接与审计记录，减少用户在两个工具之间手动传话。
- Claude Code 开发前在该文件追加任务开工记录和拟修改文件清单；完成后追加 commit、测试结果、风险说明，并标记等待 Codex 审计。
- Codex 审计时在该文件追加审计范围、结论、阻断项/非阻断项、复验命令；若有阻断项，Claude Code 继续返修并更新同一任务记录。
- 如涉及真实生产数据恢复/删除/迁移、密钥或 root 凭据、第三方平台凭据、产品经营判断、部署口径无法解释，或 Claude 与 Codex 对风险判断不一致，必须停止自动推进并标记需要第三裁判/用户介入。
- 该文件不得记录任何密钥、密码、token、证书内容、真实生产数据或可复用凭据。
