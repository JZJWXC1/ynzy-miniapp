# 寓你住一起小程序内部测试验收清单

## 测试前准备

- 后端本地测试地址：`http://127.0.0.1:3000`
- 公网内部测试地址：`http://114.55.168.97`
- 当前微信支付暂时跳过，积分充值走后台人工确认。
- 微信开发者工具已关闭域名校验，适合内部 HTTP 测试。

## 测试账号

员工手机号可直接登录：

- `13800010001`：房源上传人
- `13800010002`：带看/成交人
- `13800010003`：群聊上传人
- `13800010006`：未实名员工，用于验证敏感信息拦截

管理员后台：

- 地址：本地 `http://127.0.0.1:3000/admin-web/`
- 公网部署后：`http://114.55.168.97/admin-web/`
- 使用你已设置的管理员账号登录，密码不要写入测试文档。

## 员工端必测流程

1. 登录/注册：手机号登录、切换账号、注册新员工。
2. 首页：城市、搜索、四大入口、地图找房、我的房源、配房小帮手。
3. 房源筛选：整租、合租、业主房源、公寓，区域/板块/小区/户型/预算筛选。
4. 地图找房：按区域筛选点位，进入房源详情。
5. 房源详情：视频展示；地址和房东联系方式默认隐藏。
6. 防跳单：未实名用户不能查看敏感信息；已实名用户确认后查看并留下足迹。
7. 普通房源上传：地址、联系方式、租金、户型、视频、分佣比例；分佣不得超过 20%；上传不加积分。
8. 我的房源：查看自己上传房源，核验房态。
9. 房源足迹：只能看“我查看的”和“我的房源被查看”，不能看到无关员工足迹。
10. 房源群：上传群聊名称和聊天截图，提交后进入待审核，不立即加积分。
11. 换群：积分不足不能换群；审核通过或充值到账后，消耗 1 积分解锁一个群。
12. 分佣：登记带看、登记成交，后台和个人分佣记录可见。
13. 积分充值：提交充值申请，后台确认后积分到账。

## 管理后台必测流程

1. 非管理员账号不能登录后台。
2. 数据总览：房源数量、区域房源数量、群聊数量、用户数量。
3. 房源管理：区域/板块/小区筛选，房态核验。
4. 敏感查看足迹：管理员可看到全公司地址/电话查看记录。
5. 分佣记录：可查看上传人、成交人、分佣比例和状态。
6. 群聊上传审核：联系上传人核对，通过后上传人 +1 积分；驳回不加积分。
7. 积分流水：群聊审核、换群、充值审核都有记录。
8. 充值账单：人工确认到账后给用户增加积分；驳回不加积分。
9. 用户/管理员账号：新增管理员、改密、启用/禁用。
10. LLM 配置：查看当前供应商、模型和测试返回。
11. 上线检查：确认 OSS、LLM、后台密钥、域名和充值模式状态。
12. 数据备份：导出当前 JSON 数据。

## 自动化验证

本地后端验证：

```bash
cd server
npm run smoke
```

公网后端验证：

```bash
node scripts/verify-api.js http://114.55.168.97
```

公网全链路验证：

```powershell
cd server
$env:SMOKE_BASE_URL='http://114.55.168.97'
$env:SMOKE_REQUIRE_REAL_LLM='1'
npm run smoke
Remove-Item Env:SMOKE_BASE_URL
Remove-Item Env:SMOKE_REQUIRE_REAL_LLM
```

注意：公网全链路验证会创建测试房源、群聊、积分和分佣记录。正式内测前建议先备份数据，跑完后按备份恢复。

切换小程序到公网内部测试：

```bash
node scripts/set-miniapp-api.js --internal-http http://114.55.168.97
```

切回本地：

```bash
node scripts/set-miniapp-api.js --local
```
