# 寓你找房小程序第一版对外验收材料

## 1. 第一版结论

第一版不追求功能多，而是验收一条可以在内部中介团队里跑通的成交闭环：

租客需求进入 -> 结构化找房 -> 地图补充找房 -> 房源视频与房态校验 -> 敏感信息查看留痕 -> 客户报备 -> 从报备发起签单 -> 管理员确认 -> 上传人固定 20% 分佣。

第一版的验收目标不是公开平台上线，也不是多角色全流程，而是确认内部中介可以高效找到真实可租房源、可以追责关键动作、可以保护房源上传人的分佣权益。

## 2. 第一版范围

### 2.1 面向角色

第一版只面向内部中介和管理员：

- 中介：找房、筛选房源、上传真实房源、报备客户、发起签单、查看自己的报备/签单/分佣/足迹。
- 管理员：查看房源、查看敏感信息足迹、审核签单、确认分佣、管理失效资产池。

### 2.2 第一版不验收的能力

以下能力可以保留历史代码或作为后续版本预留，但第一版不作为可见入口、不作为验收功能：

- 租客端公开找房流程。
- 房东端、业主端、公寓运营端。
- 房源群、换群。
- 积分、充值、微信支付。
- 复杂审批流、复杂 CRM、营销活动、公开平台运营能力。
- 生产级真实语音识别服务。第一版只要求语音转写文本或手动输入文本进入字段确认流程。

## 3. 第一版固定规则

### 3.1 底部导航

底部导航固定为四个入口：

1. 找房
2. 房源
3. 地图
4. 我的

不得出现房源群、积分、充值、换群、微信支付、租客端、房东端等入口。

### 3.2 房源上传

房源上传必须满足：

- 必须上传视频。
- 必须填写小区、租金、户型、房源标签等结构化字段。
- 房源标签用于找房匹配和筛选，例如近地铁、燃气、带阳台、电梯、独卫等。
- 前端不能让中介设置上传人分佣比例。
- 前端不能提交或修改 `brokerId`、`uploaderId`、`commissionRate` 等归属和比例字段。

### 3.3 房态维护

房态规则固定为：

- 第 3 天未更新：提醒上传人核验房态。
- 第 5 天未更新：再次提醒上传人核验房态。
- 第 7 天未更新：自动失效，前台不再展示，保留在后台资产池。

前台列表、地图、找房结果只展示有效房源。失效房源不得继续出现在前台，但必须保留在后台资产池，便于管理员核验后恢复。

### 3.4 地图展示

地图是第一版核心功能，必须满足：

- 地图只展示小区位置，不展示具体楼栋、单元、房号、完整地址和房东电话。
- 地图点必须来自真实且经过确认的小区坐标。
- 不允许用随机坐标、散列坐标、区域估算坐标、默认中心点或旧偏移坐标冒充真实位置。
- 无可靠坐标的房源可以进入普通列表，但不能进入地图。
- 地图必须显示小区有效房源套数。
- 使用筛选条件后，地图套数必须随筛选结果变化。

### 3.5 报备与签单

第一版报备保持简单：

- 客户称呼可选。
- 客户手机号必填。
- 客户手机号不得在前台、日志或后台列表中完整暴露。

签单规则固定为：

- 签单只能从报备记录发起。
- 签单只填写成交月租、房东实际支付佣金、可选备注。
- 管理员确认签单前，不生成正式分佣记录。
- 管理员确认签单后，才生成正式分佣记录。

### 3.6 分佣

上传人分佣规则固定为：

- 上传人分佣 = 房东实际支付佣金 x 20%。
- 20% 由后端固定计算，前端不能修改。
- 金额按分存储，展示时再格式化为元。
- 后端不能信任客户端提交的 `brokerId`、`uploaderId`、`commissionRate`。

## 4. 核心验收流程

### 4.1 找房助手

验收目标：中介输入租客需求后，系统先结构化识别，再让中介确认字段，确认后才匹配房源。

验收步骤：

1. 进入找房入口。
2. 输入不完整需求，例如“必须有阳台”。
3. 确认系统只追问一个关键问题，不直接推荐房源。
4. 输入完整需求，例如“滨江四千以内两室，必须有燃气”。
5. 检查确认字段是否包含预算、位置、户型、入住时间、通勤、标签/偏好。
6. 修改字段后点击确认并匹配。
7. 检查匹配结果是否只返回有效、有视频房源。
8. 点击地图查看，确认筛选条件被带入地图页。

通过标准：

- 识别阶段不直接返回房源卡片。
- 字段不足时只追问，不强行匹配。
- 字段足够时先展示可编辑确认字段。
- 确认字段清空后，不得从原始文本把预算、区域、户型、标签重新带回。
- 确认后才进入匹配。

### 4.2 房源上传

验收目标：第一版只允许上传真实视频房源。

验收步骤：

1. 进入我的，点击上传房源。
2. 不选择视频，填写其他字段后提交。
3. 选择视频后再次提交。
4. 检查标签字段是否可以选择并随房源展示。

通过标准：

- 未上传视频时不能提交。
- 上传控件只允许视频，不允许图片冒充房源视频。
- 上传成功后房源带有视频标识。
- 上传页没有可编辑分佣比例。

### 4.3 房源列表与详情

验收目标：前台只展示有效且有视频的房源，详情页不直接暴露敏感信息。

验收步骤：

1. 进入房源列表。
2. 使用区域、租金、户型、标签筛选。
3. 打开任意房源详情。
4. 检查视频、租金、户型、小区、标签、维护状态。
5. 点击敏感信息查看。

通过标准：

- 前台列表不展示失效房源。
- 无视频历史房源不进入前台列表、匹配结果和详情页。
- 初始详情页不展示完整楼栋、单元、房号、房东电话。
- 查看敏感信息必须有明确操作，并留下足迹。

### 4.4 地图找房

验收目标：地图按真实小区坐标聚合，并显示筛选后套数。

验收步骤：

1. 进入地图页。
2. 查看地图小区点位。
3. 点击小区点位，查看小区名称、租金区间、房源套数。
4. 使用租金、户型、租赁方式等筛选条件。
5. 从找房助手带条件跳转地图。
6. 检查无可靠坐标房源是否不进入地图。

通过标准：

- 地图只展示小区点，不展示具体房号和房东联系方式。
- 小区点必须是可靠坐标。
- 筛选后房源套数正确变化。
- 无可靠坐标房源不能进入地图。

### 4.5 报备

验收目标：客户报备足够简单，并能作为签单前置条件。

验收步骤：

1. 打开一套有效房源详情。
2. 点击先报备客户，再从报备发起签单。
3. 不填写客户手机号，提交报备。
4. 填写客户手机号，客户称呼留空，提交报备。

通过标准：

- 客户手机号为空时不能提交。
- 客户称呼可以为空。
- 报备成功后可进入签单流程。
- 客户手机号展示必须脱敏。

### 4.6 签单

验收目标：签单只能从报备发起，并且字段保持最小。

验收步骤：

1. 完成一条客户报备。
2. 从报备记录发起签单。
3. 不填写成交月租，提交。
4. 不填写房东实际支付佣金，提交。
5. 填写成交月租、房东实际支付佣金和可选备注后提交。
6. 尝试使用旧的直接房源签单路径绕过报备。

通过标准：

- 未完成报备不能提交签单。
- 成交月租必填。
- 房东实际支付佣金必填。
- 备注可选。
- 旧的直接房源签单路径必须被后端拒绝。
- 前端不提交 `brokerId`、`uploaderId`、`commissionRate`。

### 4.7 后台确认与分佣

验收目标：管理员确认签单后才生成正式分佣记录。

验收步骤：

1. 中介提交签单。
2. 管理员进入后台签单审核。
3. 确认签单前检查分佣记录。
4. 管理员确认签单。
5. 再次查看分佣记录。

通过标准：

- 管理员确认前不生成正式分佣记录。
- 管理员确认后生成正式分佣记录。
- 上传人分佣固定为房东实际支付佣金的 20%。
- 分佣金额按分计算和存储。
- 后台确认签单时不提交 `brokerId`、`uploaderId`、`commissionRate`。

### 4.8 房态规则

验收目标：3/5/7 房态规则在前端、后端、Mock、文档中保持一致。

验收步骤：

1. 准备第 3 天、第 5 天、第 7 天未维护的测试房源。
2. 检查第 3 天是否提醒。
3. 检查第 5 天是否再次提醒。
4. 检查第 7 天是否自动失效。
5. 检查失效房源是否从列表、地图、找房结果隐藏。
6. 检查后台资产池是否保留失效房源。

通过标准：

- 第 3 天提醒。
- 第 5 天再次提醒。
- 第 7 天自动失效。
- 前台只展示有效房源。
- 失效房源保留在后台资产池。

## 5. 自动化验收命令

第一版提交验收前，必须在项目根目录执行：

```bash
node server/scripts/v1-final-audit.js
node server/scripts/v1-acceptance-check.js
node server/scripts/map-v1-test.js
node server/scripts/assistant-v1-test.js
node server/scripts/backend-contract-v1-test.js
```

全部通过后，才能进入微信开发者工具人工验收。

## 6. 微信开发者工具人工验收

人工验收必须覆盖以下模块，并保存截图或录屏：

| 模块 | 验收结论 | 证据 | 备注 |
| --- | --- | --- | --- |
| 底部导航 | 待验收 | 截图 |  |
| 上传视频房源 | 待验收 | 截图/录屏 |  |
| 找房字段确认 | 待验收 | 截图/录屏 |  |
| 房源列表与详情 | 待验收 | 截图/录屏 |  |
| 地图套数与筛选 | 待验收 | 截图/录屏 |  |
| 报备 | 待验收 | 截图/录屏 |  |
| 签单 | 待验收 | 截图/录屏 |  |
| 后台确认分佣 | 待验收 | 截图/录屏 |  |
| 房态 3/5/7 | 待验收 | 数据/截图 |  |
| 敏感信息留痕 | 待验收 | 截图/日志片段 |  |
| 隐藏入口 | 待验收 | 搜索/截图 |  |

## 7. 不通过标准

出现以下任一情况，不建议通过第一版验收：

- 底部导航不是找房、房源、地图、我的。
- 前台出现房源群、积分、充值、换群、微信支付入口。
- 上传房源可以不传视频。
- 找房助手不经字段确认直接匹配。
- 地图使用随机坐标、估算坐标或默认中心点展示房源。
- 地图展示具体楼栋、单元、房号或房东电话。
- 失效房源仍出现在前台列表、地图或找房结果。
- 签单可以绕过报备。
- 前端可以提交或修改 `brokerId`、`uploaderId`、`commissionRate`。
- 管理员确认前已经生成正式分佣记录。
- 上传人分佣不是房东实际支付佣金的 20%。
- 客户手机号、房东电话、微信号等敏感信息完整出现在前台或日志中。
- 仓库中出现真实密钥、AccessKey、LLM Key 或生产 Token。

## 8. 上线前仍需人工确认

以下事项不属于代码脚本能完全证明的范围，上线前需要人工确认：

- 微信开发者工具模拟器和真机点击验收完成。
- 测试后端或正式后端地址已配置。
- 微信小程序后台已配置合法 request 域名和 uploadFile 域名。
- OSS、LLM、后台管理员 Token 和账号密码通过环境变量或部署平台配置，没有写入仓库。
- 管理员后台可以查看报备记录、签单审核、分佣记录和敏感信息足迹。
- 历史页面源码和后端历史接口虽保留，但第一版构建和前台入口不暴露。

## 9. 第一版验收结论模板

验收人填写：

- 验收日期：
- 验收环境：
- 小程序版本：
- 后端环境：
- 验收结论：通过 / 不通过 / 有条件通过
- 阻断问题：
- 非阻断风险：
- 复验时间：

第一版只有在核心闭环可以完成时才视为通过：找房确认、地图套数、视频房源、敏感留痕、报备、签单、后台确认、固定 20% 分佣。
