# 寓你住一起小程序接口上线说明

## 接口调用流程

小程序页面不再直接读取本地模拟数据，统一通过 `utils/api-service.js` 调用接口。

- 当前 `utils/deploy-config.js` 中 `baseUrl` 默认请求本地后端 `http://127.0.0.1:3000`。
- 正式上线时，将 `baseUrl` 改为正式 HTTPS API 域名；可运行 `node scripts/set-miniapp-api.js https://你的API域名` 自动写入。
- 内部先测试且暂时只有阿里云 HTTP 公网入口时，可运行 `node scripts/set-miniapp-api.js --internal-http http://公网入口地址` 自动写入；该方式只用于开发者工具内测，不作为正式发布配置。不要直连 `3000`，统一走 Nginx 公网入口，避免影响机器人路由。
- 写入前建议先运行 `node scripts/verify-api.js http://公网入口地址`，确认公网后端、房源接口和 OSS 上传策略能访问。
- 当前 `paymentMode` 固定为 `manual`，小程序不会调用 `wx.login` 或 `/mini/auth/wechat-openid`；以后启用微信支付并完成专项验收时再改为 `wechat`。
- 小程序只请求自有后端接口，不直接携带 LLM API Key。

## 小程序接口清单

| 模块 | 方法 | 路径 | 说明 |
| --- | --- | --- | --- |
| 首页房源 | GET | `/mini/home/listings` | 首页推荐房源 |
| 房源列表筛选 | GET | `/mini/listings` | 支持分类、区域、板块、小区、户型、最高租金筛选 |
| 快速匹配 | POST | `/mini/listings/match` | 按预算、区域、户型匹配房源 |
| 配房助手 | POST | `/mini/llm/match` | 后端调用已配置 LLM，再结合房源库返回推荐 |
| 地图找房 | GET | `/mini/map/pins` | 地图房源点位 |
| 房源详情 | GET | `/mini/listings/:id` | 房源详情 |
| 房源足迹 | GET | `/mini/listings/:id/footprints` | 单套房源足迹 |
| 敏感查看 | POST | `/mini/listings/:id/sensitive-view` | 查看地址/电话并留痕 |
| 我的足迹 | GET | `/mini/footprints` | 当前用户足迹列表 |
| 我的房源 | GET | `/mini/my/listings` | 当前用户上传房源 |
| 我的房源核验 | POST | `/mini/my/listings/:id/verify` | 上传人确认房源仍可租，刷新房态核验时间 |
| 我的页面 | GET | `/mini/profile` | 用户、积分、充值、统计 |
| 微信 openid 绑定 | POST | `/mini/auth/wechat-openid` | 预留微信支付接入；`manual` 模式禁止调用，仅 `wechat` 模式启用 |
| 积分充值 | POST | `/mini/points/recharge` | 当前创建后台人工确认充值申请；以后切到微信支付时返回 `wx.requestPayment` 参数 |
| 房源群 | GET | `/mini/groups` | 群聊、积分、群内房源 |
| 群聊上传 | POST | `/mini/groups/listings` | 提交群聊名称和聊天截图，进入待审核，不立即加积分 |
| 换群 | POST | `/mini/groups/:id/unlock` | 消耗 1 积分解锁群聊 |
| 普通上传 | POST | `/mini/listings` | 上传普通房源，不增加积分 |
| 视频上传凭证 | POST | `/mini/uploads/video-policy` | 后端生成阿里 OSS 直传凭证，返回 `uploadUrl`、`formData`、`fileUrl` |
| 群聊截图上传凭证 | POST | `/mini/uploads/group-screenshot-policy` | 后端生成群聊截图 OSS 直传凭证 |
| 管理员登录 | POST | `/admin/auth/login` | 返回后台访问 Token，非管理员禁止进入 |
| 新增管理员 | POST | `/admin/accounts` | 创建后台账号，密码加密存储 |
| 管理员改密 | POST | `/admin/accounts/:id/password` | 管理员修改后台账号密码，禁止默认密码，新密码加密存储 |
| 管理员启禁 | POST | `/admin/accounts/:id/status` | 启用或禁用后台账号，禁止禁用当前登录账号 |
| 数据备份 | GET | `/admin/data/export` | 管理员导出当前业务 JSON 数据备份 |
| 环境变量模板 | GET | `/admin/env-template` | 管理员查看缺失环境变量模板，不返回 Secret 明文 |
| 上线检查 | GET | `/admin/launch-check` | 管理员查看 OSS、LLM、后台密钥、默认密码和微信域名待办 |
| 后台房态核验 | POST | `/admin/listings/:id/verify` | 管理员联系核对后刷新房态核验时间 |
| 群聊审核 | POST | `/admin/groups/uploads/:id/review` | 管理员联系上传人核对后，通过则给上传人 +1 积分 |
| 微信支付回调 | POST | `/wechat/pay/notify` | 预留微信支付成功回调；当前人工充值模式可暂不配置 |
| 充值审核 | POST | `/admin/recharges/:id/review` | 管理员确认人工充值到账或驳回 |
| 服务探活 | GET | `/healthz` | 进程存活检查，返回 200 |
| 就绪检查 | GET | `/readyz` | 上线配置就绪检查，缺配置返回 503 |

## 视频存储

房源视频不存放在小程序本地，也不直接写入数据库。正式流程为：

1. 小程序选择视频。
2. 小程序请求 `/mini/uploads/video-policy`。
3. 后端根据阿里 OSS 配置生成直传凭证。
4. 小程序用 `wx.uploadFile` 上传到 OSS。
5. 上传成功后，房源提交接口 `/mini/listings` 保存 `videoUrl` 和 `videoKey`。

## 群聊上传审核

群聊上传不是直接加积分。正式流程为：

1. 员工填写群聊名称。
2. 员工上传群聊聊天信息界面截图。
3. 小程序请求 `/mini/uploads/group-screenshot-policy`，并用 `wx.uploadFile` 上传截图到 OSS。
4. 小程序调用 `/mini/groups/listings` 提交 `title`、`screenshotUrl`、`screenshotKey`。
5. 记录进入 `待审核`，积分不变。
6. 管理员在后台联系上传人核对。
7. 管理员通过 `/admin/groups/uploads/:id/review` 审核通过后，系统写入 `pointLogs`，上传人 +1 积分到账。

## 积分充值

第一版暂时跳过微信支付，积分充值走后台人工确认：

1. 员工在“我的”页面选择充值积分，金额按 `20 元/分` 计算，`1 分起充`。
2. 小程序调用 `/mini/points/recharge` 创建充值申请，账单状态为 `待确认`。
3. 管理员收到线下收款或公司内部确认后，在 Web 后台“充值账单”点击通过。
4. 后端调用 `/admin/recharges/:id/review` 写入 `pointLogs`，积分到账。
5. 管理员驳回时，账单状态变为 `已驳回`，不会增加积分。

当前服务端保持：

```env
RECHARGE_PAYMENT_MODE=manual
```

以后切回微信支付时，再将 `RECHARGE_PAYMENT_MODE` 改为 `wechat`，并补齐：

- `WECHAT_APP_SECRET`
- `WECHAT_PAY_MCH_ID`
- `WECHAT_PAY_API_V3_KEY`
- `WECHAT_PAY_CERT_SERIAL_NO`
- `WECHAT_PAY_PRIVATE_KEY_PATH`
- `WECHAT_PAY_PLATFORM_CERT_PATH`
- `WECHAT_PAY_PLATFORM_CERT_SERIAL_NO`（可选，但建议填写）
- `WECHAT_PAY_NOTIFY_URL`

微信支付、积分充值链路属于历史保留/后续预留，第一版不作为可见入口上线；相关后端代码可保留，但验收时不进入中介端主流程。

客户端门禁位于 `app.bindWechatOpenid()` 方法自身：`manual` 模式直接结束，避免登录页等任一调用者绕过启动门禁；`wechat` 模式才获取临时 code 并绑定。获取 code 或接口响应期间若账号会话已变化，迟到响应不更新当前用户。

## 房态核验提醒

普通房源上传成功后会写入 `lastVerifiedAt`。第一版房态规则固定为第 3 天提醒、第 5 天再次提醒、第 7 天未更新自动失效：

1. 第 3 天未核验时提醒上传人核验房态。
2. 第 5 天未核验时再次提醒上传人核验房态。
3. 第 7 天仍未核验的房源自动失效，并进入后台资产池。
4. 上传人可调用 `/mini/my/listings/:id/verify` 确认仍可租。
5. 管理员后台“数据总览”显示待核验房源数量，“房源管理”表支持一键核验。
6. 管理员核验会调用 `/admin/listings/:id/verify`，同步写入房源足迹，方便后台追踪。
7. 管理员可在后台资产池将已核对房态的房源重新上架。

当前已按你提供的地址作为默认 OSS 管理入口：

`https://bj33856.apps.aliyunfile.com/disk/admin/home`

正式直传还需要补齐 Bucket、Region、AccessKey 或 STS 临时凭证。

## LLM 配置

Web 管理后台新增“LLM配置”菜单，可配置：

- 模型供应商：本地模拟、OpenAI 兼容、DeepSeek、通义千问、智谱、自定义 HTTP。
- 接口协议：OpenAI Chat Completions、Responses 风格、自定义 JSON。
- API Base URL。
- 模型名称。
- 密钥保存名。
- 系统提示词。

正式上线建议：

- 管理后台保存配置到后端数据库或配置中心。
- API Key 只保存在服务端环境变量或密钥管理系统。
- 小程序只调用 `/mini/llm/match`。
- 后端在 `/mini/llm/match` 内读取 LLM 配置、调用模型、再结合房源库返回匹配结果。

## 上线检查

Web 管理后台新增“上线检查”菜单，读取 `/admin/launch-check` 自动检查：

1. OSS Bucket、Region、AccessKey、Public Base URL 是否齐全。
2. `ADMIN_TOKEN_SECRET` 是否已使用服务端环境变量配置。
3. 管理员账号是否仍在使用内测默认密码。
4. LLM 是否启用真实供应商，且服务端是否存在对应密钥环境变量。
5. 基础用户、房源、房源群数据是否存在。
6. 微信 request/upload 合法域名作为人工确认项；积分充值当前为后台人工确认，微信支付参数不阻塞第一版上线。

接口不会返回任何 Secret 明文，只返回是否缺少对应环境变量。
部署平台探活建议使用 `/healthz`；上线前人工或流水线就绪检查使用 `/readyz`。

## 上线前安全清单

- OSS Bucket 建议保持私有读，视频和群聊截图通过后端生成短期签名地址访问。
- AccessKey Secret 只允许放在服务端环境变量或密钥管理系统，不允许写入小程序端、前端页面或公开文档。
- 已在本地 `.gitignore` 排除 `server/.env`，上线前仍建议轮换一次已经用于开发调试的 AccessKey。
- 微信小程序后台需要配置合法请求域名和上传域名：后端 API 域名、OSS Bucket 外网 Endpoint。
- Web 管理后台已接入 Token 鉴权，正式部署时请设置 `ADMIN_TOKEN_SECRET`，并在后台把默认管理员密码改掉；新密码会加密存储。
- Web 管理后台可导出当前 JSON 数据备份，上线前和每日运营结束后建议备份。
- 房源群、积分、充值、微信支付属于历史保留/后续预留，第一版中介端与后台可见入口均隐藏。
- 房源第 3 天提醒、第 5 天再次提醒、第 7 天未核验必须自动失效并进入后台资产池，管理员核对后可重新上架。
