[Unit]
Description=每天三次检查 Ynzy 飞书房源同步

[Timer]
# 显式绑定北京时间，每天只在 08:00、14:00、20:00 触发三次。
# 不设置 OnBootSec，也不补跑关机期间错过的日历任务，确保每个自然日最多只有三个自动触发点。
OnCalendar=*-*-* 08,14,20:00:00 Asia/Shanghai
AccuracySec=1s
Persistent=false
Unit=ynzy-feishu-sync.service

[Install]
WantedBy=timers.target
