# 上线记录（2026-07-03）

## 范围

- 分支：`v1-broker`
- Commit 范围：`7186d93..d884644`
- 主要内容：V3 上线前加固合入、生产部署配置补齐、`FOOTPRINT_MAX_ROWS=30000`、部署包默认不带 `.env`、`server/data/`、`server/certs/`。

## 公网验证结果

| 验证项 | 命令 | 实际返回码 | 结论 |
| --- | --- | --- | --- |
| 历史积分充值接口下线 | `curl -X POST https://zf-api.ynzyqbot.cn/mini/points/recharge` | `404` | 通过 |
| 历史房源群接口下线 | `curl https://zf-api.ynzyqbot.cn/mini/groups` | `404` | 通过 |
| 第一版房源列表接口 | `curl https://zf-api.ynzyqbot.cn/mini/listings` | `200` | 通过 |
| 探活接口 | `curl https://zf-api.ynzyqbot.cn/healthz` | `200` | 通过 |

## 证书处置

- 商户证书已于 2026-07-03 10:43 作废。
- 第一版不启用微信支付，不申请新证书。
- V2 启用微信支付前再申请新证书，并重新配置服务器证书路径。
- 上线后需清理服务器 `server/certs/` 下已作废证书文件，避免误用。

## 遗留 TODO

- 迁库：当前 `FOOTPRINT_MAX_ROWS=30000` 是 JSON 单文件模式下的上线限额；90 天以上留存或更高并发追责需求，后续迁移正式数据库和归档表。
- `smoke-test` 改造：历史 smoke 流程与第一版下线规则存在遗留差异，后续单独改造，保持本轮红线不触碰 `server/scripts/smoke-test.js`。
- V2 支付证书：第一版不启用支付；V2 开启微信支付前重新申请商户证书、配置新证书并完成支付回归。
