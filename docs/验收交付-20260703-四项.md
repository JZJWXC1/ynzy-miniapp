# 2026-07-03 真机验收四项交付记录

## 一、首页快照修复 + 可见性最终边界

- 公司房源快照、列表、详情统一公开完整字段；匿名与登录快照返回同一份表格数据。
- 合作房源匿名详情保持 401；合作房源地址与电话仍走实名、需求单和留痕机制。
- 首页快照按接口表头动态渲染；后端快照保证表头与数据列数一致，并向下填充合并区域/小区列。
- 本地 curl 快照列清单：区域、小区、房号、户型描述、户型分类、押一付一、押二付一、看房方式密码、备注。

## 二、遗留清尾

- 新增统一位置映射：闸弄口、新塘、元宝塘、东站归上城区，其余已配置板块归拱墅区。
- 飞书同步创建和更新都会回写 `district/area`；快照兜底公司房源也使用同一映射。
- 新增一次性回填脚本：`server/scripts/backfill-listing-districts.js`。
- 本地回填结果：总数 55，变更 55，拱墅区 50，上城区 5，待分区 0。
- `district=上城区` 本地验证：5 条，板块为闸弄口/新塘/元宝塘/东站组合，非法板块 0。
- 服务器 `.env` 需要配置：`FEISHU_SYNC_INTERVAL_MINUTES=60`。

## 三、房源列表页筛选栏

- 公司房源页新增区域、板块、小区、户型、租金区间筛选。
- 区域默认拱墅区，可切上城区/不限；板块随区域联动；小区支持输入联想。
- 户型支持不限、一室、两室、三室、三室以上；服务端按 `layout` 解析室数。
- 价格区间继续使用 `rentMin/rentMax`。

## 四、组合验证与部署包

- 本地接口组合 1：`/mini/listings?district=上城区&block=闸弄口&rentMode=整租`，返回 5 条；样本 `L1782580265065533`，琬秋铭府，整租一室。
- 本地接口组合 2：`/mini/listings?district=拱墅区&block=东新园&layout=两室&rentMin=3000&rentMax=5000`，当前本地数据返回 0 条。
- 全量测试：所有独立 `*-test.js` 通过；`smoke-test.js` 保持既有输出，打印若干 FAIL 但退出成功，未修改。
- 部署包：`dist/ynzy-miniapp-deploy.zip`。
- 部署包安全扫描：81 个条目，未发现 `.env`、`server/data`、`certs`、lark 参数 JSON、pem/key/secret 匹配项。
