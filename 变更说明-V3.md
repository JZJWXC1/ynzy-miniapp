# V3 累计包 · 变更说明（2026-07-02 第三轮，响应外部审计意见）

本包为**三轮累计包**（17 个文件），按相同路径一次性覆盖即可，替代此前 V1/V2 包。
本轮针对外部审计（GPT5.5）提出的 P1/P2 逐条修复，并把复现步骤固化为回归用例。

## 审计意见处置

### [P1] 库外小区裸提交绕过审核 → 已修复 ✅
- `server/src/domain.js`：无声明分支由"新建默认已匹配"改为**服务端小区库权威判定**：
  `communityMatched = serverKnownCommunity || grandfatheredMatched`。
  新建库外小区一律 `未匹配 + 待审核`；编辑且小区名未变仍沿用历史判定（飞书存量不受影响）。
- 按审计原步骤复现验证（`UnknownCommunityABC` 裸提交，不带任何匹配/审核字段）：
  修复前 `status=待确认 / reviewStatus=无需审核 / homeListings=1`；
  修复后 **`status=待审核 / reviewStatus=待审核 / requiresManualReview=true / homeListings=0`**。
- 复现已固化为 `server/scripts/backend-contract-v1-test.js` 末尾的正式回归用例，
  含首页/前台列表/地图三处不可见断言 + 管理员审核通过后可见的正向断言。

### [P2] 历史接口下线默认不生效 → 已修复 ✅
- `server/src/config.js`：`V1_DISABLE_LEGACY_ROUTES` 默认值翻转为 **true**。
  生产忘配环境变量也安全；如需临时恢复历史功能，显式设 `V1_DISABLE_LEGACY_ROUTES=0`。
- 默认配置实测：`/mini/points/recharge` 404、`/mini/groups` 404、`/mini/listings` 200。

### [建议] 足迹留存 5000 条是否满足审计要求 → 已可配置 ✅
- 上限改为环境变量 `FOOTPRINT_MAX_ROWS` 控制（默认 5000，下限保护 1000）。
  请业务侧按追责留存周期确认取值；长期生产仍建议迁移真实数据库。

## 验收脚本 fixture 调整（P1 收紧的配套）

收紧后原脚本中"库外小区名"的正常流程 fixture 会被正确地拦进审核，导致断言失败。
按用例本意（测的是正常房源流程，而非小区匹配），将 fixture 替换为**小区库内且无坐标**
的真实小区，保持"无坐标不进地图"等原语义不变：

- `backend-contract-v1-test.js`：后端契约测试小区→半山家苑、客户端手填坐标小区→北海公园、
  默认中心点小区→财富壹号、旧偏移小区→城市风景、历史小区→浜河部落、
  提醒小区→保利香槟国际、失效小区→北景园水镜苑（京漾东韵府等有坐标 fixture 不变）
- `v1-closure-contract-test.js`：闭环测试小区→城北天邑国际
- `v1-acceptance-check.js`：第一版验收无坐标小区→半山家苑

`smoke-test.js` 依旧未触碰（受 v1-final-audit 的 git diff 防篡改检查保护）。

## 回归结果（三轮叠加后实际运行）

- 全部 32 个脚本（含 v1-acceptance-check、v1-online-gap-audit）：**31 通过**，
  唯一失败仍是改动前既有的遗留 `smoke-test.js`
- `v1-final-audit.js`：唯一失败项为 smoke-test 的 git diff 检查，系瘦身包缺 `.git`
  的环境问题；修改三个验收脚本未触发任何防篡改项
- 新增回归用例（库外小区待审核 + 三处前台不可见 + 审核通过后可见）一次通过

## 微信开发者工具验收步骤（按审计建议顺序）

1. 上传一个不在小区库的小区名 → 我的房源里应显示"待审核"，首页/房源列表/地图均不出现；
   管理后台审核通过后出现。
2. 默认配置（不设任何环境变量）启动后端 → 群、积分、换群相关接口应 404。
3. 地图只展示已确认坐标小区；筛选切换视野不回跳、套数跟随最后一次筛选。
4. 视频上传显示实时百分比；超大视频被预检拦下。
5. 首页来回切 tab，60 秒内不重复刷新飞书快照。

## V1/V2 既有内容（随包累计，不再展开）

服务端小区库、客户端只能收紧不能放宽的匹配判定、非管理员不可豁免审核、
上传 5 分钟超时+进度+压缩+大小预检、地图竞态守卫、首页节流、助手消息增量渲染、
足迹统一写入口、打包脱敏清单与部署脚本数据保护。

## 仍需人工处理

微信支付商户 API 证书（商户号 1746601037）作废重申——代码无法代办，见
`docs/打包与脱敏清单.md` 第一节。
